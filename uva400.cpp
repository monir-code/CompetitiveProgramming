#include <bits/stdc++.h>
using namespace std;
#define ll long long
int main()
{

    char ch[10000];
    ll a,c,d,b,i,j,k,l,n,ln,m,p;
    string st[100002];
    while(cin >> n){
            m=0;
        for(i=0;i<n;i++){
         cin >> st[i];
     ln=strlen(ch);
     if(m<ln)m=ln;
        }
    m+=2;
    m=62/m;
    p=n/m;
    if(p*m!=n)p++;

    }

    cout << ln << endl;
}
